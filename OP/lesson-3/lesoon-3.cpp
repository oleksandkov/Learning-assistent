#include <iostream> 
// using the while funciton 
using namespace std;

int main() {
    int sum = 0;
    int number = 1;
    //  do {
    //     cout << "Enter number or 0 to exit: ";
    //     cin >> number;
    //     sum += number;

    //  } while (number != 0); 

    //  while (number != 0) {
    //     cout << "Enter number or 0 to exit: ";
    //     cin >> number;
    //     sum += number;
    //  }

    // for (;;) {
    //     cout << "Enter number of 0 to exit: ";
    //     cin >> number;
    //     if(number <= 0) {
    //         break;
    //     }
    //     sum += number;
    // }
    //  cout << " Sum is: " << sum;

    int a, b;
    for (a = 0, b = 20000; a <b; a++, b -=2) {

    }
    cout << " meeting: " <<  a;
     cin.get();
     cin.get();
}