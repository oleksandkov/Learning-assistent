# Global Project Standards
- Always use TypeScript instead of plain JavaScript.
- Prefer functional programming patterns over object-oriented classes.
- Write unit tests for every new utility function using Jest.
- Never commit hardcoded secrets or API keys.