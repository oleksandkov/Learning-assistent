---
description: 'React component standards'
applyTo: 'src/components/**/*.tsx'
---
# React Standards
- Always use React hooks (`useState`, `useEffect`).
- Ensure all props are explicitly typed.
- Avoid using inline styles; strictly use CSS modules.