{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "*",
        "type": "command",
        "command": "powershell -NoProfile -Command \"Add-Content -Path .github/hooks/hook.log -Value ('PostToolUse fired at ' + (Get-Date -Format o))\""
      }
    ]
  }
}